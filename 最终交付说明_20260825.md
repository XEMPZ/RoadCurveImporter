# RoadCurveImporter 最终交付说明

**版本状态：** 已完成正式发布。`RoadCurveImporter.ps1` 的 SHA-256 为：

```text
4FA051A122FFCF911F058C64F99C525C24E70C3189C18FA88AF8E9CA60702932
```

本交付包含：SouthMap/ZWCAD 与 AutoCAD 的只读道路要素读取、直线/圆弧/多段线处理、严格 0.5 mm 与宽松 2 mm 欧拉回旋线识别、道路要素表、测量员八列 Excel、可缩放示意图、标准 AC1015/R2000 DXF 还原，以及已经由用户真实资源管理器双击验证的 SouthMap/ZWCAD 冷启动文件关联修复。

## 使用方法

请双击 `启动道路曲线要素导入器.cmd`。该启动器会以 PowerShell 7 的 STA 模式运行 WPF 导入器。使用期间 CAD 图纸只读：程序不会保存、修改、删除或关闭您的 CAD 图形。

导出 DXF 时，已识别的欧拉缓和曲线将按拟合模型加密写成开放 `LWPOLYLINE`，图层为 `ROAD_SPIRAL_POLYLINE`，最大段长 0.25 m。真实 SouthMap/ZWCAD 验收确认复杂测试文件可见 16 条拟合缓和曲线。

## 已固定的 CAD 冷启动状态

现在 `.dwg` 和 `.dxf` 的当前用户 UserChoice 关联指向：

```text
wscript.exe → SouthMapColdOpen.vbs → SouthMapColdOpen.ps1 → StartCAD.exe → ZwLauncher.exe
```

这使 CAD 未运行时的资源管理器双击先建立正确的 SouthMap OEM 会话，再加载图纸；CAD 已运行时仍按原厂 `ZwLauncher.exe` 路径转交图纸。用户已对真实资源管理器双击确认成功。

> 请保持整个项目目录位置不变，尤其不要删除 `SouthMapColdOpen.vbs`、`SouthMapColdOpen.ps1` 与关联脚本。若迁移目录，请在新目录重新执行 `install_independent_southmap_cold_open_userchoice.ps1`，并在资源管理器中真实双击测试图纸确认后，再删除旧目录。

## 核心文件

| 文件 | 说明 |
|---|---|
| `RoadCurveImporter.ps1` | 正式 WPF 道路曲线要素导入器。 |
| `dxf_writer.py` | 基于 ezdxf 的标准 DXF 写出器。 |
| `RoadCurveImporter_技术手册.md` | 完整架构、参数、维护和回退手册。 |
| `real_cad_visual_acceptance_20260825.md` | DXF 在 SouthMap/ZWCAD 中的真实可视验收记录。 |
| `SouthMap_ZWCAD_冷启动无响应研究与修复_20260825.md` | 直接双击冷启动问题的根因、方案与验收记录。 |
| `rollback_independent_southmap_cold_open_userchoice.ps1` | 将 DWG/DXF 关联恢复到首次安装前状态。 |

## 回滚

如需撤销冷启动关联修复，请先关闭所有 SouthMap/ZWCAD 窗口，再运行：

```powershell
pwsh -NoProfile -ExecutionPolicy Bypass -File ".\rollback_independent_southmap_cold_open_userchoice.ps1"
```

DXF 导出器如需回退，正式版前的备份仍保留在项目目录中；具体路径、哈希与回退前检查见技术手册第 12 节。

## 验收依据

最终交付已完成如下证据闭环：复杂回旋 DXF 标准解析、道路之星 dxflib 结构对比、16 条回旋线 0.25 m 采样回归、SouthMap/ZWCAD 真实可视加载、Windows Shell 冷启动测试，以及用户真实资源管理器双击成功确认。
